console.log("Hola desde index para Pancho");

const add = (x, y) => {
  return x + y;
};
const substract = (x, y) => {};
const multiply = (x, y) => {
  return x * y;
};
const divide = (x, y) => {
  if (y === 0) {
    return;
  }
  return x / y;
};
